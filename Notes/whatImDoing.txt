SVM:First thought of using an support vector machine to classify the radar signals, wrote up a program to display this 
and realized that svm are usually used for only 2 categories of classifcation and the number of categories needed to be more
flexible and not fixed at to so I decided to opt for a decisionTree model

DecisionTree: Opted for this as it can to multi classes meaniong more than 2 which is exactly what is needed